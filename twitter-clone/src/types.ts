export type User = {
  username: string;
  id: string;
  name: string;
  email: string;
};

export type UserData = {
  id: string;
  name: string;
  email: string;
  password: string;
};

export type TweetData = {
  author_id: string;
  text: string;
};
